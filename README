TDVT guidance: https://tableau.github.io/connector-plugin-sdk/docs/tdvt

Setup:
    1. install TDVT: python3 -m pip install tdvt-1.5.14.tar.gz
    2. set or make sure the following flags are set to your maxcompute project:
        odps.sql.validate.orderby.limit=false
        odps.sql.decimal.odps2=true
        odps.sql.type.system.odps2=true
        odps.sql.timezone=UTC
    3. update config/odps_config.ini, set 'CommandLineOverride' to be the path to unzipped 'plugin' directory
    4. update config/odps_config.ini, set 'tablePrefix' to be your maxcompute project name
    4. edit config/tdvt/tdvt.ini and set the path to tabquerytool
    5. move the jdbc driver (see lib/) to the tableau driver directory (like ~/Library/Tableau/Drivers)
    6. setup dataset, see dataset directory and TDVT guidance for detail 
    6. follow the TDVT guidance to run tests
    
How to use odpscmd:
    1. you can execute odpscmd/bin/odpscmd to enter the interactive shell, the id and key have been well configured
    2. each command has to be ended with a ';'
    3. view the metadata of a table with 'desc' command (like 'desc calcs;')
    4. you can execute SQL statements generated by TDVT directly (like 'SELECT * FROM TBL LIMIT 50;')
    5. use 'help;' command for more advanced commands

Tips:
    1. the log of jdbc is in the same directory of jdbc driver
    2. official doc of odpscmd: https://www.alibabacloud.com/help/doc-detail/27804.html
    3. to generate tds file, you could use the scripts/start.sh to start Tabluea desktop with maxcompute plugin. see the script for detail
